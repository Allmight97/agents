interface:
  display_name: "To Issues"
  short_description: "Split approved plans into issues"
  default_prompt: "Use $to-issues to turn this approved plan into vertical-slice GitHub issues."

policy:
  allow_implicit_invocation: true
